# M0–M3 release handoff — 2026-09-25

## Where the work lives

- Norn: draft [PR #76](https://github.com/antiartificial/norn/pull/76), branch `codex/v3-m0-m3-release-integration`. Keep remaining Norn M0–M3 implementation in this grouped PR with focused commits.
- Fleet: companion draft [PR #176](https://github.com/antiartificial/norn-fleet/pull/176). It is separate because Fleet is a separate repository.
- Neither draft is approved for protected-master merge or live Mini/Fleet deployment. No v3 deployment, provider cutover, or milestone sign-off has occurred.

## Implemented and verified locally

- **M0:** Mini v2 source, binary, schema, and topology inventory; read-only growth samples; private-data schema/restore rehearsals; guarded baseline and backup verification on a private copy. A fresh [current-head private-copy rehearsal](m5-mini-current-head-private-copy-2026-09-25.md) passed through migration 38, compatibility 5/30, original-column full-row preservation, and passive startup against 249,197 copied Mini rows. Live Mini remains on v2 and unchanged.
- **M1:** shared signed acceptance, generation-fenced claims, atomic auth revocation, supervised effects, archive-gated reconciliation, and a runtime mutation fence. Selected PostgreSQL, Nomad, crash/race, and two-API tests pass locally.
- **M2:** named database catalog and resolver; verified-TLS MySQL/WordPress path; guarded source snapshot and restore foundations. Private source admission derives a signed request from successful deployment provenance and live Nomad observation. The source stop rechecks exact revision/allocations before mutation. Claim supervision spans stop, runtime-account lock, dump staging, and retained-artifact publication. Migrations 29–35 cover source reservation, stop, account lock, signed v1 staging receipt, restore binding, atomic source-to-restore fence transfer, and signed v2 retention receipt. The streaming local artifact adapter bounds a descriptor at 64 GiB and verifies content. Its retention proof is **node-local**.
- **M2 WordPress source quiescence:** a pinned stock WordPress 6.8.2 container with a verified TLS hook, native disposable MySQL 8.0.27, PostgreSQL 16, Nomad 2.0.7, and Consul 2.0.4 passed the opt-in signed deployment-to-source flow. The private source acceptance binds the exact launched cold-start reservation; the claimed runner performs a guarded Nomad CAS stop, atomically records the source stop and launch reservation as stopped, locks the runtime MySQL account, and stages a signed SQL dump containing the marker row. The default wrong-CA WordPress startup test remains separate. This local fixture is not MySQL 8.4, a real object provider, a restore of the WordPress artifact, or Mini release qualification.
- **M2 retained restore path (in progress):** the private restore worker materializes a signed retained object into an owner-only file under its supervised claim, uses that file for the SQL import, and rejects a retained source when no object store is supplied. Prepare and Begin preflight the verified materialized file while the signed historical staging path remains provenance. A PostgreSQL-backed test removes the staged file and rejects a tampered retention receipt. A disposable PostgreSQL 16 plus MySQL 8.0.27 run proved prepare, account lock, SQL import, and target verification after deleting the staged file. The same test now publishes through an S3-compatible adapter and restores through a second client with a separate private spool, using the in-process S3 emulator. A separate 9 MiB emulator test proves that conditional creation reaches multipart completion; the pinned high-level SDK drops that header, so the adapter uses its lower-level Core API. Publishers sharing an upload spool now serialize across processes and count abandoned upload files against the total configured capacity; an operator must inspect and remove stale files after a crash. These tests prove the remote-client code path, not a real object provider, separate-node runtime, or off-host durability.
- **M3:** implemented normal Fleet API routes use etcd without control PostgreSQL; disposable three-member etcd TLS/RBAC, quorum, snapshot, and two-API rehearsals have run. Live Fleet bootstrap and fault qualification remain open.

The full PostgreSQL-backed `v2/api/store`, `v2/api/pipeline`, and `v2/api/worker` suites passed after the WordPress source handoff. PR #76 CI passed at `37248bf`. A focused disposable PostgreSQL 16/MySQL 8.0.27 test passed with S3 emulator publication and a separate restore client. The prior disposable Nomad 2.0.7 guarded-stop and MySQL process-kill tests passed. Fleet PR #176's `contract` job did not start because GitHub reported an account payment/spending-limit issue; local Fleet host tests passed earlier.

## Next implementation slice

1. **Provider and cross-node retained restore:** the opt-in [provider qualification](m2-s3-provider-qualification.md) is ready for a dedicated immutable bucket and scoped credential; it has not been run against a real provider. Qualify the S3 adapter there, then restore a signed MySQL artifact from another node after local staged bytes are removed. Add an explicit durable materialization checkpoint if the worker must recover the private file across a restart; current failures before SQL remain fenced for inspection.
2. **Signed recovery:** complete operator-observed resume/unlock with generation and lineage checks; qualify failed/ambiguous stop, account lock, publication, transfer, import, and cleanup boundaries.

## Gates before M0–M3 exit and release

- **M0:** representative sanitized Mini workload fixture and full-cycle growth; numeric retention/backup/restore budgets; app/route/volume/database ownership exceptions; exact signed candidate and retained backup; private Mini upgrade and rollback rehearsal.
- **M1:** remaining concrete PostgreSQL consumers and inline external effects; actual Mini launchctl/cloudflared recovery; function and mutable-app crash/race checks on the release runtime.
- **M2:** remote retention plus cross-node materialization; signed recovery; least-privilege fence account; large-data bounds; managed WordPress/MySQL restore and rollback; direct Nomad, cron/function, host assurance, and provider lifecycle qualification.
- **M3:** live protected OIDC/GitHub and real clients with two APIs; separate-node supervised bootstrap, member/certificate lifecycle, partition and disk faults, restore, and soak; clear the Fleet CI account gate.
- **Release:** signed immutable Norn/Fleet artifacts, reviewed protected-master merges, and explicit deployment/rollback decisions. M4–M9 capacity, upgrade, migration, and adoption work follows these contracts.

Keep implementation evidence distinct from live qualification. The PR descriptions carry the broader change inventory; the dated slice notes in this directory contain detailed test and safety boundaries.
